import React, { Component } from "react";
import Header from './commons/Header'

export default class InputComponent extends Component {
  render() {
    return (
        <Header title={"Bem vindo à nossa bela aplicação"}/>
    );
  }
}